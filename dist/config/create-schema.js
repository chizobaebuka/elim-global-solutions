"use strict";
// import { sequelize } from '../lib/db/sequelize';
// import './src/models'; // ensure models are imported and registered
// import { logger } from '../lib/logger/logger';
// (async () => {
//     try {
//         await sequelize.sync({ alter: true }); // dev only - adjust for prod
//         logger.info('DB Schema created/updated (sync alter)');
//         process.exit(0);
//     } catch (err) {
//         logger.error('Failed to sync DB schema', err);
//         process.exit(1);
//     }
// })();
